# >>> coga-managed >>>
# Managed by `coga init`. Don't edit between these markers —
# they will be overwritten. Add your own ignore rules below the
# end marker.
# Machine-local config — paths and secrets. Never commit.
coga.local.toml

# Vendored CLI — written by `coga init` from upstream.
.coga/

# Generated merged skill view for agent CLIs. Rebuilt by `coga init`
# and `coga launch`.
.agent-skills/

# Per-domain scaffolding templates (bare `_template`, `_template.md`).
**/_template/
**/_template.md
# <<< coga-managed <<<
